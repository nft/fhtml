[package]
name = "fhtml"
version = "0.1.0"
edition = "2021"
description = "Compiler for fhtml (Fluid HTML) — a token-cheap, Tailwind-native markup language"
license = "MIT"

[lib]
name = "fhtml"
path = "src/lib.rs"

[[bin]]
name = "fhtml"
path = "src/main.rs"
